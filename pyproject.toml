[tool.black]
line-length = 88

[tool.ruff]
select = ["E", "F", "W", "C90"]

[tool.pytest.ini_options]
addopts = "-v"

[project]
name = "cnn_object_detector"
version = "0.1.0"
requires-python = ">=3.10"
